"use strict";
const $ = selector => document.querySelector(selector);const $$ = selector => [...document.querySelectorAll(selector)];
const storage = {  get(key, fallback) {    try {      const value = localStorage.getItem(key);      return value === null ? fallback : JSON.parse(value);    } catch {      return fallback;    }  },  set(key, value) {    try {      localStorage.setItem(key, JSON.stringify(value));      return true;    } catch {      return false;    }  },  remove(key) {    try {      localStorage.removeItem(key);      return true;    } catch {      return false;    }  }};
// Редактируемые примеры направлений.// Оставьте только то, что соответствует вашим реальным возможностям.const directions = [  {    id: "interfaces", category: "design", symbol: "◈",    title: "Дизайн интерфейсов",    description: "Визуальная структура, понятная навигация и внимание к пользовательскому опыту.",    tag: "UI / UX"  },  {    id: "web", category: "tech", symbol: "⌘",    title: "Веб-проекты",    description: "Страницы, интерактивные элементы и адаптивные цифровые пространства.",    tag: "Веб"  },  {    id: "automation", category: "tech", symbol: "⇄",    title: "Автоматизация",    description: "Поиск способов сократить повторяющиеся действия и упростить процессы.",    tag: "Процессы"  },  {    id: "identity", category: "design", symbol: "✳",    title: "Визуальная концепция",    description: "Цвет, типографика и единый характер оформления проекта.",    tag: "Визуальный стиль"  },  {    id: "texts", category: "content", symbol: "✎",    title: "Тексты и структура",    description: "Понятные формулировки, логичная подача информации и работа со смыслом.",    tag: "Контент"  },  {    id: "research", category: "content", symbol: "⌕",    title: "Исследование идей",    description: "Сбор информации, сравнение подходов и формулирование следующих шагов.",    tag: "Анализ"  },  {    id: "prototypes", category: "design", symbol: "◇",    title: "Прототипирование",    description: "Первый набросок будущего продукта для обсуждения и проверки идеи.",    tag: "Концепции"  },  {    id: "tools", category: "tech", symbol: "⊞",    title: "Полезные инструменты",    description: "Подбор цифровых решений под конкретные задачи и рабочие привычки.",    tag: "Инструменты"  },  {    id: "presentation", category: "content", symbol: "↗",    title: "Упаковка проекта",    description: "Структура презентации, главные аргументы и ясная история проекта.",    tag: "Презентация"  }];
const savedFavorites = storage.get("space-favorites", []);const favorites = new Set(  Array.isArray(savedFavorites)    ? savedFavorites.filter(id => directions.some(item => item.id === id))    : []);
let activeFilter = "all";let toastTimer;
function notify(message) {  clearTimeout(toastTimer);  $("#toast").textContent = message;  $("#toast").classList.add("visible");  toastTimer = setTimeout(() => $("#toast").classList.remove("visible"), 4000);}
// Темаfunction applyTheme(theme) {  document.documentElement.dataset.theme = theme;  $("#theme").setAttribute(    "aria-label",    theme === "dark" ? "Включить светлую тему" : "Включить тёмную тему"  );}
const savedTheme = storage.get("space-theme", null);applyTheme(  savedTheme === "dark" || savedTheme === "light"    ? savedTheme    : matchMedia("(prefers-color-scheme: light)").matches ? "light" : "dark");
function toggleTheme() {  const next = document.documentElement.dataset.theme === "dark" ? "light" : "dark";  applyTheme(next);  storage.set("space-theme", next);}$("#theme").addEventListener("click", toggleTheme);
// Мобильное менюfunction closeMenu() {  $("#navigation").classList.remove("open");  $("#menu").setAttribute("aria-expanded", "false");  $("#menu").setAttribute("aria-label", "Открыть меню");}
$("#menu").addEventListener("click", () => {  const open = $("#navigation").classList.toggle("open");  $("#menu").setAttribute("aria-expanded", String(open));  $("#menu").setAttribute("aria-label", open ? "Закрыть меню" : "Открыть меню");});
$$(".nav-links a").forEach(link => link.addEventListener("click", closeMenu));
// Карточки создаются безопасно через DOM.const cardElements = new Map();
directions.forEach(item => {  const card = document.createElement("article");  card.className = "card";
  const top = document.createElement("div");  top.className = "card-top";
  const symbol = document.createElement("span");  symbol.className = "card-symbol";  symbol.textContent = item.symbol;  symbol.setAttribute("aria-hidden", "true");
  const star = document.createElement("button");  star.className = "icon star";  star.type = "button";  star.addEventListener("click", () => {    const wasSelected = favorites.has(item.id);    wasSelected ? favorites.delete(item.id) : favorites.add(item.id);
    const saved = storage.set("space-favorites", [...favorites]);    updateCards();
    // Если карточка исчезла из избранного, возвращаем фокус к фильтру.    if (card.hidden) $('[data-filter="favorites"]').focus();
    notify(saved      ? wasSelected ? "Удалено из избранного" : "Добавлено в избранное"      : "Выбор изменён, но сохранить его в браузере не удалось");  });
  const title = document.createElement("h3");  title.textContent = item.title;
  const description = document.createElement("p");  description.textContent = item.description;
  const tag = document.createElement("span");  tag.className = "tag";  tag.textContent = item.tag;
  top.append(symbol, star);  card.append(top, title, description, tag);  $("#cards").append(card);  cardElements.set(item.id, { card, star });});
function updateCards() {  const query = $("#search").value.trim().toLocaleLowerCase("ru");  let visible = 0;
  directions.forEach(item => {    const { card, star } = cardElements.get(item.id);    const selected = favorites.has(item.id);    const categoryMatches =      activeFilter === "all" ||      (activeFilter === "favorites" ? selected : item.category === activeFilter);    const queryMatches = `${item.title} ${item.description} ${item.tag}`      .toLocaleLowerCase("ru").includes(query);
    card.hidden = !(categoryMatches && queryMatches);    if (!card.hidden) visible++;
    star.textContent = selected ? "★" : "☆";    star.setAttribute("aria-pressed", String(selected));    star.setAttribute(      "aria-label",      `${selected ? "Убрать из избранного" : "Добавить в избранное"}: ${item.title}`    );  });
  $("#empty").hidden = visible !== 0;  $("#result-count").textContent = `Найдено направлений: ${visible}`;  $("#favorite-count").textContent = favorites.size;
  const titles = directions.filter(item => favorites.has(item.id)).map(item => item.title);  $("#selected-summary").textContent = titles.length    ? titles.join(" · ")    : "Направления пока не выбраны.";}
function setFilter(filter) {  activeFilter = filter;  $$("[data-filter]").forEach(button => {    button.setAttribute("aria-pressed", String(button.dataset.filter === filter));  });  updateCards();}
$$("[data-filter]").forEach(button => {  button.addEventListener("click", () => setFilter(button.dataset.filter));});$("#search").addEventListener("input", updateCards);$("#reset-filters").addEventListener("click", () => {  $("#search").value = "";  setFilter("all");  $("#search").focus();});updateCards();
// Черновикconst draftFields = ["name", "goal", "deadline"];const draft = storage.get("space-draft", {});
if (draft && typeof draft === "object" && !Array.isArray(draft)) {  draftFields.forEach(id => {    if (typeof draft[id] !== "string") return;    const field = $(`#${id}`);
    if (id === "deadline") {      if ([...field.options].some(option => option.value === draft[id])) {        field.value = draft[id];      }    } else {      field.value = draft[id].slice(0, field.maxLength);    }  });}
let draftWarningShown = false;
$("#brief-form").addEventListener("input", () => {  const data = Object.fromEntries(draftFields.map(id => [id, $(`#${id}`).value]));  if (!storage.set("space-draft", data) && !draftWarningShown) {    notify("Локальное сохранение недоступно. Скачай запрос, чтобы не потерять его.");    draftWarningShown = true;  }  $("#copy-fallback").hidden = true;});
$("#clear-draft").addEventListener("click", () => {  if (!confirm("Удалить текст черновика? Избранное останется.")) return;  $("#brief-form").reset();  $("#copy-fallback").hidden = true;  const removed = storage.remove("space-draft");  notify(removed ? "Черновик очищен" : "Поля очищены, но удалить сохранённый черновик не удалось");});
function buildRequest() {  const titles = directions.filter(item => favorites.has(item.id)).map(item => item.title);  return [    "ЗАПРОС НА ОБСУЖДЕНИЕ",    "",    `Имя: ${$("#name").value.trim()}`,    `Направления: ${titles.length ? titles.join(", ") : "Нужна помощь с выбором"}`,    `Желаемый срок: ${$("#deadline").value}`,    "",    "Описание задачи:",    $("#goal").value.trim()  ].join("\n");}
$("#brief-form").addEventListener("submit", async event => {  event.preventDefault();  $("#copy-fallback").hidden = true;
  if (!$("#name").value.trim() || !$("#goal").value.trim()) {    notify("Заполни имя и описание задачи — не только пробелами.");    return;  }
  const text = buildRequest();
  if (event.submitter?.value === "download") {    const blob = new Blob(["\uFEFF", text], { type: "text/plain;charset=utf-8" });    const url = URL.createObjectURL(blob);    const link = document.createElement("a");    link.href = url;    link.download = "my-request.txt";    document.body.append(link);    link.click();    link.remove();    setTimeout(() => URL.revokeObjectURL(url), 10000);    notify("Файл подготовлен для скачивания");    return;  }
  try {    await navigator.clipboard.writeText(text);    notify("Запрос скопирован. Его можно вставить в письмо или сообщение.");  } catch {    $("#copy-fallback").hidden = false;    $("#copy-text").value = text;    $("#copy-text").focus();    $("#copy-text").select();    notify("Скопируй выделенный текст вручную");  }});
// Быстрые действияconst commands = $("#commands");
function openCommands() {  if (!commands.open) commands.showModal();}
$("#command-open").addEventListener("click", openCommands);$("#command-close").addEventListener("click", () => commands.close());
document.addEventListener("keydown", event => {  if ((event.ctrlKey || event.metaKey) && event.code === "KeyK") {    event.preventDefault();    commands.open ? commands.close() : openCommands();  }  if (event.key === "Escape" && $("#navigation").classList.contains("open")) {    closeMenu();    $("#menu").focus();  }});
$$("[data-action]").forEach(button => {  button.addEventListener("click", () => {    const action = button.dataset.action;    commands.close();
    if (action === "theme") {      toggleTheme();      return;    }
    if (action === "favorites") {      $("#search").value = "";      setFilter("favorites");    }
    const target = action === "brief" ? $("#brief") : $("#directions");    target.scrollIntoView({      behavior: matchMedia("(prefers-reduced-motion: reduce)").matches ? "instant" : "smooth"    });
    // Переносим клавиатурный фокус к выбранному разделу.    target.setAttribute("tabindex", "-1");    target.focus({ preventScroll: true });    target.addEventListener("blur", () => target.removeAttribute("tabindex"), { once: true });  });});
// Прогресс чтенияfunction updateProgress() {  const max = document.documentElement.scrollHeight - innerHeight;  const percent = max > 0 ? Math.min(100, Math.max(0, scrollY / max * 100)) : 0;  $("#progress").style.width = `${percent}%`;}
addEventListener("scroll", updateProgress, { passive: true });addEventListener("resize", updateProgress);new ResizeObserver(updateProgress).observe(document.body);
$("#year").textContent = new Date().getFullYear();updateProgress();